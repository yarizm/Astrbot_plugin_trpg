{
  "admin_user_ids": {
    "description": "允许执行导入、发布、重置的管理员用户 ID 列表。",
    "type": "list",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "max_import_chars": {
    "description": "单次导入允许的最大 Markdown 字符数。",
    "type": "int",
    "default": 20000
  },
  "max_list_size": {
    "description": "列表命令单次最多返回多少条剧本。",
    "type": "int",
    "default": 20
  },
  "db_filename": {
    "description": "SQLite 数据库文件名，将保存在插件数据目录下。",
    "type": "string",
    "default": "trpg.sqlite3"
  },
  "bootstrap_builtin_scenarios": {
    "description": "插件启动时是否自动初始化内置剧本包。开启后，首次启动会自动上架内置剧本，后续不会重复导入。",
    "type": "bool",
    "default": true
  },
  "scenario_export_dir": {
    "description": "剧本 Markdown 导出目录，相对于插件数据目录。",
    "type": "string",
    "default": "scenarios"
  },
  "solo_max_steps": {
    "description": "单人跑团 LLM agent 每轮最大工具调用步数。",
    "type": "int",
    "default": 10
  },
  "solo_system_prompt_override": {
    "description": "自定义单人跑团系统提示词（为空则使用内置默认）。",
    "type": "string",
    "default": ""
  }
}
